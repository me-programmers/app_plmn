<%= render '/layouts/confirm_modal'%>

<%= form_tag  pasien3s_path, :method => 'get', :id => "ajax_search_form" do %>
<div class="page-header">
    <h1 class="pull-left">pasien3s</h1>
    <div class="pull-right">
        <%= link_to new_pasien3_path, :remote => true,
        :class => "btn btn-primary", :style => "margin-right:10px; height:15px;" do%>
            <i class="icon-star icon-white"></i> Add New pasien3
        <% end %>
        
        <div id="quicksearch" class="search-box">
            <%= text_field_tag :search, params[:search], :placeholder => "Search"  %>
        </div>
        <%= hidden_field_tag :ajax_search, "true" %>
    </div>
</div>
<div class="row-fluid">
    <div id="table_div" class="span12">
        <%= render :partial => 'pasien3s_table' %>
    </div>
</div>
<% end %>
